application:
  cache:
    key-prefix: CredentialsTest
    time-to-live:
      verification-request: 60
      verified-session: 60
      credential-metadata: 60
  environment: local
  signature:
    secret-key: test-secret-key
